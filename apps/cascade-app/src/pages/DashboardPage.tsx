/**
 * Purpose: Dashboard placeholder page — replaced by E-03 feature content in a later sprint.
 * Inputs:  None.
 * Outputs: <main> with h1 heading for route identification.
 * Constraints: Placeholder only; do not add business logic here.
 * SPORT: MASTER-ROUTES.md — /dashboard
 */

export function DashboardPage() {
  return (
    <main className="flex-1 p-6">
      <h1 className="text-2xl font-semibold">Dashboard</h1>
    </main>
  );
}
