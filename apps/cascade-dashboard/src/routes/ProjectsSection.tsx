/**
 * Purpose: Placeholder route for the Projects section (per-project context view).
 * Inputs: none.
 * Outputs: JSX page placeholder with section heading.
 * Constraints: Will be replaced in P3/P4 with real project-context content.
 * SPORT: T-P3-E02-04 layout shell routes
 */
export function ProjectsSection() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-foreground mb-2">Projects</h1>
      <p className="text-muted-foreground text-sm">
        Active projects, cascade context, and cross-repo status.
      </p>
    </div>
  )
}
