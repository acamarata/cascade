/**
 * Purpose: Placeholder route for the Global section (cross-account fleet view).
 * Inputs: none.
 * Outputs: JSX page placeholder with section heading.
 * Constraints: Will be replaced in P3/P4 with real fleet-status content.
 * SPORT: T-P3-E02-04 layout shell routes
 */
export function GlobalSection() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-foreground mb-2">Global</h1>
      <p className="text-muted-foreground text-sm">
        Fleet-wide status across all accounts and sessions.
      </p>
    </div>
  )
}
