# Cascade — strict Clippy configuration
# Run: cargo clippy --workspace --all-targets -- -D warnings

msrv = "1.85"

# Enforce documentation on all public items
# Applied via: #![deny(missing_docs)] in each crate lib.rs
