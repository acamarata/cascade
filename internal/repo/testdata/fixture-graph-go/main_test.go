package main

// Placeholder sibling test file, mirroring the internal/plugins/testdata/
// conformance/proc_stub pattern (Go's toolchain ignores testdata/ trees
// entirely, so this file never affects SymbolGraph extraction).
