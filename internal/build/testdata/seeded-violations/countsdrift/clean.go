// Package fixture is the counts drift gate's GREEN fixture: it states the
// same numbers the frozen taxonomy and closed domain set actually have
// (14-kind taxonomy, twelve-domain set), so a correct run of
// CheckCountsDrift must find zero violations here.
package fixture
