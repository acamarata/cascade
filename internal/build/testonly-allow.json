[
  {
    "symbol": "internal/context.Discover",
    "reason": "Role-anchored tier discovery ships ahead of the surface that resolves a working directory. Its merge consumer (the tier-merge ticket) has since landed and takes an already-resolved []TierRecord, so it does not call Discover either; the first production caller is the composition root that turns a cwd into a merged instruction context, which is the harness-generation / context-assembly ticket, not this one.",
    "expected_caller": "the harness instruction-generation surface (04-PEWS-PLAN-W1-W3.md Wave 2 Epic E S-08 T3) or context assembly (S-09.T1), whichever first turns a working directory into a merged instruction context; retire this entry together with internal/context.MergeTiers"
  },
  {
    "symbol": "internal/context.MergeTiers",
    "reason": "The merge model is specified against an already-resolved []TierRecord, so its only caller is the surface that pairs it with Discover. That surface does not exist yet: no ticket before this one turns a working directory into a merged instruction context. Building the precedence engine before its consumer is the right order, since the consumer is specified against this result type; shipping a consumer that assembles instructions without a decided precedence rule would not be.",
    "expected_caller": "the harness instruction-generation surface (04-PEWS-PLAN-W1-W3.md Wave 2 Epic E S-08 T3), which calls Discover then MergeTiers to render a harness instruction file; retire this entry and the internal/context.Discover entry together when it lands"
  },
  {
    "symbol": "internal/rpc.ElevationMiddleware",
    "reason": "The elevation gate is built before the verbs it gates. No elevated verb is registered on the daemon yet, so installing it today would gate nothing. Building the guard first is the right order. Leaving it uninstalled once an elevated verb exists would not be.",
    "expected_caller": "buildRPCServer in cmd/cascade/daemon_unix_run.go, in the SAME change that registers the first elevated verb"
  },
  {
    "symbol": "internal/rpc.NewNonceLedger",
    "reason": "Constructed only by the elevation middleware, which is not installed yet. Retire together with the ElevationMiddleware entry.",
    "expected_caller": "buildRPCServer in cmd/cascade/daemon_unix_run.go, alongside ElevationMiddleware"
  },
  {
    "symbol": "internal/runtime.DaemonlessElevationPrecondition",
    "reason": "Evaluates the injected precondition for the daemonless guard and shares its fate.",
    "expected_caller": "the daemonless execution path, once an elevated verb can be invoked without a daemon"
  },
  {
    "symbol": "internal/runtime.NewBaselineChecker",
    "reason": "Built and tested; no caller on the daemon start path. The doctor command cannot construct one either: it needs a provider.Store, an EventPublisher and an AuditRecorder, none of which the CLI layer opens.",
    "expected_caller": "the daemon start path, or a doctor.Check adapter over the baseline checker once the daemon exposes the store it needs"
  },
  {
    "symbol": "internal/runtime.DaemonLogsHandler",
    "reason": "Subsystem is built and tested. Its command surface is scheduled for a later wave, so nothing that ships calls it yet. Listed so the state is visible rather than assumed.",
    "expected_caller": "the daemon logs command"
  },
  {
    "symbol": "internal/runtime.Bootstrap",
    "reason": "Deliberate and documented: cmd/cascade/daemon_unix.go calls runtime.Scan directly and explains why in a comment at its call site. Listed for visibility, not as a defect.",
    "expected_caller": "none by design; revisit only if that comment's reasoning changes"
  },
  {
    "symbol": "internal/runtime.DaemonlessStateFrom",
    "reason": "Daemonless state accessor, used once daemonless verbs read it.",
    "expected_caller": "daemonless command implementations"
  },
  {
    "symbol": "internal/runtime.OpenEmbeddedReadStore",
    "reason": "The read-only open used by the daemonless read path, awaiting the first daemonless read verb.",
    "expected_caller": "daemonless read verb implementations"
  },
  {
    "symbol": "internal/runtime.NewFixedClock",
    "reason": "A deterministic clock provided for tests in other packages. Shipping code correctly uses the system clock.",
    "expected_caller": "none; test-facing by design, kept out of a test-only package because production types reference the Clock interface"
  },
  {
    "symbol": "internal/daemon.NewConnTracker",
    "reason": "The connection tracker is constructed inside the serve path rather than by name from outside.",
    "expected_caller": "none directly; retire if the serve path stops using the type"
  },
  {
    "symbol": "internal/storage.Export",
    "reason": "Subsystem is built and tested. Its command surface is scheduled for a later wave, so nothing that ships calls it yet. Listed so the state is visible rather than assumed.",
    "expected_caller": "the export command"
  },
  {
    "symbol": "internal/storage.Import",
    "reason": "Subsystem is built and tested. Its command surface is scheduled for a later wave, so nothing that ships calls it yet. Listed so the state is visible rather than assumed.",
    "expected_caller": "the import command"
  },
  {
    "symbol": "internal/storage.SetExportClock",
    "reason": "Determinism seam for export, unused until export has a command.",
    "expected_caller": "the export command"
  },
  {
    "symbol": "internal/storage.NewCapabilityRegistry",
    "reason": "Cross-domain capability grants are built and not consulted by any shipped path yet.",
    "expected_caller": "the storage composition root, once a plugin requests a cross-domain grant"
  },
  {
    "symbol": "internal/storage.ReservedPluginHostNamespace",
    "reason": "Reserved-namespace constant, consulted once plugin storage namespacing is enforced at runtime.",
    "expected_caller": "the plugin host storage path"
  },
  {
    "symbol": "internal/storage.StorageHealthCheck",
    "reason": "Adapting this into a doctor.Check needs a live *sql.DB, which the doctor command has no handle on: nothing in cmd/ opens the storage database, and the adapter itself belongs in internal/storage, not in the CLI layer.",
    "expected_caller": "the storage doctor.Check adapter, registered from productionCheckRegistry in cmd/cascade/doctor.go once storage exposes one"
  },
  {
    "symbol": "internal/storage/queue.New",
    "reason": "Subsystem is built and tested. Its command surface is scheduled for a later wave, so nothing that ships calls it yet. Listed so the state is visible rather than assumed.",
    "expected_caller": "the queue consumer, scheduled with its command surface"
  },
  {
    "symbol": "internal/events.EventKindPluginRegistered",
    "reason": "An event kind defined ahead of the plugin registration path that will publish it.",
    "expected_caller": "the plugin registration path"
  },
  {
    "symbol": "internal/hooks.NewRegistry",
    "reason": "Subsystem is built and tested. Its command surface is scheduled for a later wave, so nothing that ships calls it yet. Listed so the state is visible rather than assumed.",
    "expected_caller": "the daemon run path, once hooks are dispatched"
  },
  {
    "symbol": "internal/hooks.NewDispatcher",
    "reason": "Subsystem is built and tested. Its command surface is scheduled for a later wave, so nothing that ships calls it yet. Listed so the state is visible rather than assumed.",
    "expected_caller": "the daemon run path, once hooks are dispatched"
  },
  {
    "symbol": "internal/doctor.NewMCPIntegrationCheck",
    "reason": "The mcp_integration check takes a HarnessDiscoverer. No production implementation of that interface exists anywhere in the tree, so the doctor command cannot mount this check without a hand-written stand-in that would probe nothing (Art.1). The command's composition root (productionCheckRegistry in cmd/cascade/doctor.go) names the gap in a comment.",
    "expected_caller": "productionCheckRegistry in cmd/cascade/doctor.go, in the SAME change that lands a real HarnessDiscoverer"
  },
  {
    "symbol": "internal/doctor.NewSubsystemCensusCheck",
    "reason": "The subsystem_census check takes a SubsystemStateProvider. Nothing in the tree implements DeclaredSubsystems/RunningSubsystems, so the doctor command cannot mount this check against a real data source yet.",
    "expected_caller": "productionCheckRegistry in cmd/cascade/doctor.go, in the SAME change that lands a real SubsystemStateProvider"
  },
  {
    "symbol": "providers/postgres.Open",
    "reason": "The postgres provider is selectable by configuration, but no shipped code path resolves a provider by name yet, so nothing constructs it. Postgres is therefore unreachable in a running daemon regardless of configuration.",
    "expected_caller": "the provider resolution path that maps a configured driver name to its Open function"
  },
  {
    "symbol": "internal/retrieval.ChunkerFor",
    "reason": "This ticket (ingest/chunk pipeline) ships MarkdownChunker and CodeChunker plus the ChunkerFor extension-to-chunker selector, but the file-system walk that reads real files off disk and dispatches them into ChunkerFor is a separate, not-yet-built ticket (T2 owns the storage write path per this ticket's own HOW section). Nothing that ships walks a directory and calls ChunkerFor yet.",
    "expected_caller": "the ingest-walk entry point that reads a repo's files and calls ChunkerFor(path) before Chunk-ing each one, landing with the T2 FTS5-indexing ticket"
  },
  {
    "symbol": "internal/retrieval/corpus.NewStore",
    "reason": "The corpus and scope model ships ahead of every consumer that reads it. Query-time fusion, the recall command surface, the retrieval config surface, context assembly, the team capability and grant wiring and the auto-advance ceiling are all later tickets, and each one is the thing that would construct a store. Building the classification and membership rules first is the right order, since those consumers are specified against this model; shipping a consumer that reads content without it would not be.",
    "expected_caller": "the retrieval fusion path that applies a scope filter before its query legs, and the context assembly path that takes a session scope as a required input"
  },
  {
    "symbol": "internal/retrieval/rrf.Fuse",
    "reason": "The ranking core is built before the two surfaces that call it. The citations model and the recall command are separate tickets in this sprint, and until one of them exists nothing that ships asks for a fused ranking. Building the ranker first is the right order; leaving it unreachable once a recall surface exists would not be.",
    "expected_caller": "the recall command surface (Epic F sprint 11 ticket 3), which runs both legs and fuses them"
  },
  {
    "symbol": "internal/retrieval/rrf.DefaultK",
    "reason": "The smoothing constant the recall surface will pass into Fuse. It is unreachable for exactly as long as Fuse is, and is asserted against the value the retrieval reference documents rather than against a second copy of itself.",
    "expected_caller": "the recall command surface, in the same change that first calls Fuse"
  },
  {
    "symbol": "internal/retrieval/rrf.StrategyFTS",
    "reason": "The full-text leg's name. The vector leg names itself and ships; the full-text leg is a different ticket in the preceding sprint and has not landed, so nothing in the running program labels a list with this name yet.",
    "expected_caller": "the full-text index leg (Epic F sprint 10 ticket 2), which labels its ranked list with it"
  },
  {
    "symbol": "internal/retrieval/fusion.NewScopeFilter",
    "reason": "The one scope-enforcement mechanism in retrieval, built before the query path that applies it. It has to exist before either leg can be narrowed by it, which is why it lands first; it must not stay unreachable once a query path exists, because an unapplied scope filter enforces nothing.",
    "expected_caller": "the recall command surface (Epic F sprint 11 ticket 3), which resolves the filter before running either leg"
  },
  {
    "symbol": "internal/retrieval/fusion.NewVectorLeg",
    "reason": "The query-time vector leg, built alongside the scope filter that binds it. Its production caller is the same query path, and it shares that path's fate.",
    "expected_caller": "the recall command surface, in the same change that resolves the scope filter"
  },
  {
    "symbol": "internal/retrieval/citations.Assemble",
    "reason": "The citation model ships one ticket ahead of the surfaces that show a citation. Assembling provenance has to exist before the recall surface can render it and before context assembly can attribute what it assembled, which is why it lands first; nothing that ships asks for a citation set yet. It must not stay unreachable once a recall surface exists, because an answer that cites nothing is the state this model exists to end.",
    "expected_caller": "the recall command surface and its --cite flag (04-PEWS-PLAN-W1-W3.md Wave 2 Epic F S-11.T3), which assembles the set from the fused results it just ranked, and the MCP search response built on the same call"
  },
  {
    "symbol": "internal/retrieval/citations.Render",
    "reason": "The Markdown footnote form of an assembled citation set. It is unreachable for exactly as long as Assemble is, and shares its fate: the only two consumers of the rendered form are the --cite output and the MCP search response, neither of which exists yet.",
    "expected_caller": "the recall command surface's --cite output (Epic F S-11.T3), in the same change that first calls Assemble"
  },
  {
    "symbol": "internal/memory.NewProjectionJob",
    "reason": "The projection is the memory store's derived read side, built one ticket before the surfaces that run it. Nothing that ships constructs one yet because the memory CLI verbs and the recall fusion path are later tickets; both need a projection to exist before they can query or schedule it, which is why it lands first. It must not stay unconstructed once either exists, because a projection nothing runs indexes nothing.",
    "expected_caller": "the memory CLI verbs (04-PEWS-PLAN-W1-W3.md Wave 2 Epic G S-13.T3), which run the projection behind remember/recall, and the recall fusion path (Epic F S-11), which queries the rows it writes"
  },
  {
    "symbol": "internal/memory.NewFileStore",
    "reason": "This ticket delivers the files-first memory store itself: the record types, the on-disk format and the CRUD surface. Nothing constructs a store yet because the surfaces that would are later tickets in the same sprint, which this one is the chain head for. Building the store before its callers is the right order; leaving it unconstructed once the CLI verb exists would not be.",
    "expected_caller": "the memory CLI verbs (04-PEWS-PLAN-W1-W3.md Wave 2 Epic G S-13.T3, memory.remember / memory.recall), and the database projection built over the same tree (Epic G S-13.T2), each of which constructs a store rooted at the configured memory path"
  },
  {
    "symbol": "internal/audit.HashParams",
    "reason": "The audit log is built before the subsystems that write to it. HashParams is how a caller records what an action's parameters were without recording the parameters themselves, so it becomes reachable in the same change that gives policy, the approval queue, config reload, and the elevation middleware their injected audit.Writer. Wiring it earlier would mean inventing a caller that records nothing real.",
    "expected_caller": "the policy decision path (04-PEWS-PLAN-W1-W3.md Epic I S-17.T2) and the elevation middleware (Epic D S-06.T3), in the change that injects audit.Writer into each"
  },
  {
    "symbol": "internal/audit.ParseFilter",
    "reason": "The token form of the query filter exists for the command line to hand its flags in. This ticket owns the query engine and is explicitly told not to build the CLI or RPC surface, so the only caller that can exist today is the test that proves the parser refuses an unknown field instead of widening the result set.",
    "expected_caller": "the `cascade policy audit query` command surface (04-PEWS-PLAN-W1-W3.md Epic I S-18.T6)"
  },
  {
    "symbol": "internal/policy.NewCommandClassifier",
    "reason": "The classifier is built one ticket before the evaluator that consumes it. Its own contract assigns the wiring to the deny-list and evaluation ticket in the same sprint, which injects the CommandClassifier seam that resolves a level once before the policy layers run. Constructing it from a caller that then discarded the level would prove nothing, so the state is recorded here instead of faked.",
    "expected_caller": "the policy evaluation engine's injected CommandClassifier seam (04-PEWS-PLAN-W1-W3.md Epic I S-17.T4)"
  },
  {
    "symbol": "internal/memory.NewFileCandidateLedger",
    "reason": "The candidate ledger is built one ticket before the two surfaces that drive it. Nothing that ships opens a memory store base directory for candidates yet, so constructing one from a caller that then discarded it would prove nothing.",
    "expected_caller": "the memory review-queue CLI (04-PEWS-PLAN-W1-W3.md Epic G S-14.T3) and the forget pipeline (Epic G S-14.T4), which construct the ledger over the same store base the memory CLI already resolves"
  },
  {
    "symbol": "internal/memory.NewPromotionLadder",
    "reason": "The mechanical promotion lane is complete and tested, but the consolidation surface that records observations during a session is a later ticket. Retire this entry together with the ledger constructor entry.",
    "expected_caller": "the memory review-queue CLI (04-PEWS-PLAN-W1-W3.md Epic G S-14.T3), which observes and reviews candidates through the ladder"
  },
  {
    "symbol": "internal/audit.New",
    "reason": "The audit log is built and its only intended caller, the vault audit verb, deliberately refuses to print an empty list while nothing injects a writer. Wiring follows when a subsystem actually records events.",
    "expected_caller": "cmd/cascade/vault_audit.go, once a writer is injected into internal/secrets"
  },
  {
    "symbol": "internal/retrieval/embed.New",
    "reason": "The embed pipeline is complete; the ingest and recall command surface that drives it is a later ticket in this epic.",
    "expected_caller": "the recall and index command surface, P1-E06-W2-S11-T3"
  },
  {
    "symbol": "internal/storage/cache.New",
    "reason": "The cache domain is implemented against the storage interfaces; no shipping path opens a cache yet.",
    "expected_caller": "the composition root that opens the store, when a subsystem needs caching"
  },
  {
    "symbol": "providers/fs.New",
    "reason": "A storage provider implementation. Nothing resolves a provider by name in shipping code yet, which is the same gap recorded for the postgres provider.",
    "expected_caller": "the provider resolution path that maps a configured driver name to its constructor"
  },
  {
    "symbol": "providers/localvector.New",
    "reason": "The local vector provider. Same gap: no shipping path resolves a vector provider by configured name yet.",
    "expected_caller": "the provider resolution path, alongside the embed pipeline's consumer"
  }
]