{
  "provenance": {
    "dataset": "cascade-docs-known-item v1.0.0",
    "source_repo": "acamarata/cascade-v1 (archived, read-only per .claude/planning/p1/ARCHIVE-MAP.md)",
    "source_commit": "6c7ec3e35b0f994d0a488b2c35a678fbe59f4c42",
    "queries_from": "crates/cascade-rag/tests/fixtures/eval/queries.jsonl (generated by that repo's scripts/gen-eval-corpus.py from its own 89 public documents)",
    "documents_from": "the same repo's docs/ and .github/ trees, copied verbatim",
    "method": "known-item retrieval; ground truth is the SOURCE PATH the query's passage was lifted from, not any engine's output",
    "harvested": "2026-09-05",
    "tokenizer": "exact-token unicode-lowercase alphanumeric (internal/retrieval.tokenize)",
    "ranking": "BM25 k1=1.2 b=0.75, ties broken by ascending chunk id"
  },
  "documents": [
    {
      "path": "docs/target-dir-hygiene.md",
      "content": "# Build-artifact hygiene\n\n`target/` on the development machine reached **190 GB**.\n\nCargo never garbage-collects superseded artifacts. Every toolchain bump,\nfeature-flag combination and dependency update leaves its predecessors in\nplace, forever. Nothing prunes them, and nothing warns.\n\nThis is not theoretical. Accumulated Rust build output has already caused a\nsame-day disk-exhaustion kernel panic on this hardware. Recovering from that\ncosts far more than a weekly sweep.\n\n## The sweep\n\n```bash\nscripts/sweep-target.sh          # default: 14 days\nscripts/sweep-target.sh 30       # more conservative\nDRY_RUN=1 scripts/sweep-target.sh\n```\n\nIt wraps `cargo sweep --time <days>`, which removes artifacts not **accessed**\nin that window. Access time is the right axis: objects belonging to a stale\nfeature combination go, and anything the current build actually touches stays,\nso the next build is still incremental rather than from scratch.\n\nRequires `cargo install cargo-sweep`. Without it the script reports the size\nand stops — it will not guess with `rm`. Deleting the wrong thing inside\n`target/` costs a full rebuild; deleting the wrong thing outside it costs more.\n\n## Running it weekly\n\nThe script is deliberately not self-installing. Schedule it with whatever the\nmachine already uses; a LaunchAgent is the macOS option:\n\n```xml\n<!-- ~/Library/LaunchAgents/dev.cascade.sweep.plist -->\n<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<!DOCTYPE plist PUBLIC \"-//Apple//DTD PLIST 1.0//EN\"\n  \"http://www.apple.com/DTDs/PropertyList-1.0.dtd\">\n<plist version=\"1.0\">\n<dict>\n  <key>Label</key><string>dev.cascade.sweep</string>\n  <key>ProgramArguments</key>\n  <array>\n    <string>/bin/bash</string>\n    <string>REPO/scripts/sweep-target.sh</string>\n    <string>14</string>\n  </array>\n  <key>StartCalendarInterval</key>\n  <dict><key>Weekday</key><integer>0</integer><key>Hour</key><integer>3</integer></dict>\n  <key>StandardOutPath</key><string>/tmp/cascade-sweep.log</string>\n  <key>StandardErrorPath</key><string>/tmp/cascade-sweep.log</string>\n</dict>\n</plist>\n```\n\nReplace `REPO` with the checkout path, then:\n\n```bash\nlaunchctl bootstrap \"gui/$(id -u)\" ~/Library/LaunchAgents/dev.cascade.sweep.plist\n```\n\n## On a shared CARGO_TARGET_DIR\n\nConsidered and **not** adopted. Pointing every cascade checkout at one shared\n`target/` would deduplicate across clones, but it serialises builds behind a\nsingle directory lock — and the local-resource rule already forbids more than\none heavy compile against this repo at a time for exactly that reason. It also\nturns one corrupt artifact into everyone's problem.\n\nPer-checkout `target/` plus a scheduled sweep keeps the failure blast radius\nto one clone.\n"
    },
    {
      "path": ".github/wiki/Scheduler.md",
      "content": "# Scheduler\n\nThe Cascade daemon includes a built-in scheduled-task execution engine. It runs as a Tokio background task and executes shell-free child processes on cron, interval, or one-time schedules.\n\n## Overview\n\nThe scheduler polls the task store every 30 seconds (configurable) for tasks whose `next_run` is due. For each due task, it spawns a child process using an arg-array (no shell string interpolation), captures stdout and stderr, and updates the task status in the store.\n\n## Configuration\n\nAdd a `[scheduler]` section to `~/.cascade/config.toml`:\n\n```toml\n[scheduler]\nenabled = true\npoll_interval_secs = 30\nmax_parallel = 4\n```\n\n| Field | Default | Description |\n|---|---|---|\n| `enabled` | `true` | Enable the scheduled-task engine |\n| `poll_interval_secs` | `30` | Seconds between polls for due tasks |\n| `max_parallel` | `4` | Maximum tasks running simultaneously |\n\n## Schedule types\n\nThree schedule types are supported:\n\n**Cron**: runs on a cron expression (5- or 6-field):\n\n```json\n{ \"type\": \"cron\", \"expression\": \"0 9 * * *\" }\n```\n\n**Interval**: runs every N seconds after the last run:\n\n```json\n{ \"type\": \"interval\", \"secs\": 3600 }\n```\n\n**Once**: runs exactly once at a specific UTC datetime:\n\n```json\n{ \"type\": \"once\", \"at\": \"2026-06-15T09:00:00Z\" }\n```\n\n## Task lifecycle\n\n1. Task is inserted into the store with `enabled = true` and `next_run` set (or null for new tasks).\n2. On the next poll, the scheduler finds the task via `get_due(now)`.\n3. For **Once** tasks: `enabled` is set to `false` atomically in the store before the process is spawned, preventing double-run on daemon restart.\n4. The child process runs: `Command::new(command).args(args)`.\n5. On success: `status = Success { ran_at, duration_ms }`, `next_run` computed from the schedule.\n6. On failure (non-zero exit): `status = Failed { ran_at, error }` with stderr truncated to 512 chars.\n\n## Security\n\n- Commands are never run through a shell. The `command` field is passed directly to `execvp()` and `args` are individual arguments: no shell expansion, no injection surface.\n- stderr is capped at 512 characters to prevent unbounded memory allocation from runaway tasks.\n- Tasks run with the daemon user's permissions. There is no sandboxing in P2; sandboxing is planned for a future phase.\n\n## Status fields\n\n| Status | Meaning |\n|---|---|\n| `Pending` | Has not run yet |\n| `Running` | Currently executing (set transiently) |\n| `Success { ran_at, duration_ms }` | Completed with exit code 0 |\n| `Failed { ran_at, error }` | Non-zero exit or OS-level spawn error |\n\n## Parallel execution\n\nThe scheduler uses a `tokio::sync::Semaphore` with `max_parallel` permits. When the permit count is exhausted, additional due tasks wait until a running task finishes and releases its permit. Tasks are dispatched in `next_run ASC NULLS FIRST` order.\n"
    },
    {
      "path": ".github/wiki/Plugin-Development.md",
      "content": "# Plugin Development\n\nCascade plugins extend the system with new data sources, AI tools, embedding providers, and autonomous agents. Plugins are compiled to WebAssembly (WASM) and run inside a sandboxed wasmtime host.\n\nThis page is an overview. For the complete step-by-step guide including the PDK API reference and full examples, see [Plugin Development Guide](Plugin-Development.md).\n\n---\n\n## What plugins can do\n\n| Plugin kind | Description |\n|---|---|\n| `DataSource` | Ingest data from external services (GitHub Issues, Jira, Notion) into the RAG index |\n| `ChatTool` | Add callable tools to the MCP server |\n| `EmbeddingProvider` | Replace the default Multilingual E5 Large embedder with a custom model |\n| `Agent` | Autonomous sub-agent triggered by daemon events or schedules |\n\n---\n\n## How plugins work\n\nEach plugin is a `.wasm` binary paired with a `plugin.json` manifest. The manifest declares:\n\n- Plugin name, version, and kind\n- Capabilities it needs (filesystem read, network, keychain read)\n- Entry point functions\n\nAt startup, the daemon scans `~/.cascade/plugins/` and loads each `.wasm` file. Plugins run in isolated wasmtime sandboxes. Anything not declared in the manifest is denied.\n\n---\n\n## Quick start\n\nInstall the PDK and generate a new plugin from the template:\n\n```bash\ncargo install cascade-pdk-cli\ncascade-pdk new my-plugin --kind data-source\ncd my-plugin\ncargo build --target wasm32-wasip1 --release\n```\n\nInstall the built plugin:\n\n```bash\ncascade plugin install target/wasm32-wasip1/release/my_plugin.wasm\ncascade plugin list\n```\n\n---\n\n## Plugin manifest example\n\n```json\n{\n  \"name\": \"my-plugin\",\n  \"version\": \"0.1.0\",\n  \"kind\": \"DataSource\",\n  \"capabilities\": [\"network\"],\n  \"entrypoints\": {\n    \"ingest\": \"cascade_ingest\"\n  }\n}\n```\n\n---\n\n## Capability model\n\nCapabilities are declared up-front and enforced at runtime. The host denies any call not covered by the manifest.\n\n| Capability | What it allows |\n|---|---|\n| `filesystem_read` | Read files under the declared path prefix |\n| `filesystem_write` | Write files under the declared path prefix |\n| `network` | Outbound HTTP requests to declared domains |\n| `keychain_read` | Read named secrets from the OS keychain |\n| `stdin_stdout` | Read/write stdio (for pipe-based data sources) |\n\n---\n\n## Inspecting a plugin\n\nBefore installing an unknown plugin, inspect its manifest:\n\n```bash\ncascade plugin inspect ./third-party-plugin.wasm\n```\n\nThis shows the capabilities it requests without loading the plugin. Only install plugins you trust.\n\n---\n\n## Bundled plugins\n\nThese plugins ship with Cascade:\n\n| Plugin | Kind | Source |\n|---|---|---|\n| `github-issues` | DataSource | GitHub Issues → RAG |\n| `gitlab` | DataSource | GitLab Issues + MRs → RAG |\n| `jira` | DataSource | Jira tickets → RAG |\n| `linear` | DataSource | Linear issues → RAG |\n\nSource and documentation for each: [github-issues](plugins-github-issues.md), [gitlab](plugins-gitlab.md), [jira](plugins-jira.md), [linear](plugins-linear.md).\n\n---\n\n## Full guide\n\nFor the complete PDK API, ABI contract, testing harness, CI setup, and publishing instructions, see [Plugin Development Guide](Plugin-Development.md).\n"
    },
    {
      "path": "docs/coverage-baseline-2026-08-23.md",
      "content": "# Coverage baseline, 2026-08-23\n\nT-P7-E24-03: produce a real, measured coverage report and record it as a\nbaseline for future phases.\n\nMeasured, not estimated. Reproduce with:\n\n```bash\ncargo llvm-cov nextest --workspace --profile ci --summary-only\n```\n\n## Workspace total\n\n| Metric | Covered | Total | Coverage |\n|---|---:|---:|---:|\n| **Lines** | 86,380 | 113,180 | **76.32%** |\n| Regions | 137,218 | 176,407 | 77.78% |\n| Functions | 9,912 | 14,332 | 69.16% |\n\nLine coverage is the headline number: **76.32%**.\n\nFunction coverage is the lowest of the three, at 69.16%. That gap is expected\nand worth understanding rather than chasing — a crate with many small\nplatform-gated or trait-impl functions shows a low function figure while its\nexecuted lines are well covered.\n\n## Per crate, worst first\n\n| Crate | Lines | Line coverage |\n|---|---:|---:|\n| cascade-fleet-widget | 252 | 28.2% |\n| cascade-pdk | 340 | 46.8% |\n| cascade-cli | 12,026 | 52.3% |\n| cascade-app | 7,318 | 55.2% |\n| cascade-local-llm | 2,264 | 55.6% |\n| cascade-pdk-macro | 35 | 57.1% |\n| cascade-keychain | 159 | 73.0% |\n| cascade-daemon | 22,519 | 73.3% |\n| cascade-plugins | 3,741 | 77.0% |\n| cascade-security | 571 | 78.8% |\n| cascade-mcp | 7,810 | 79.7% |\n| cascade-tray | 207 | 84.5% |\n| cascade-ccapi | 551 | 84.6% |\n| cascade-providers | 7,767 | 85.6% |\n| cascade-agents | 4,122 | 85.8% |\n| cascade-rag | 12,606 | 86.0% |\n| cascade-harness | 2,655 | 86.1% |\n| cascade-core | 23,081 | 86.3% |\n| cascade-db | 611 | 87.1% |\n| cascade-types | 2,505 | 87.6% |\n| cascade-audit | 345 | 95.1% |\n| cascade-personal | 527 | 96.6% |\n\nBundled example plugins (echo-tool, clock-widget, jira, gitlab, linear,\ngithub-issues) range 57.9%–100% and are excluded from the reading below; they\nare samples, not product surface.\n\n## Reading it honestly\n\n**The low numbers are mostly where code cannot run in a test, not where it is\nuntested.**\n\n- `cascade-fleet-widget` (28.2%) is a macOS NSStatusItem menu-bar app. Its\n  `run_widget()` is a no-op off macOS and the real path needs a window server.\n- `cascade-cli` (52.3%) is 12k lines of command surface. Much of it shells out\n  to provider CLIs, installs launch agents, or drives a real daemon — the parts\n  that are pure are tested, the parts that spawn processes largely are not.\n- `cascade-app` (55.2%) is the Tauri backend; its commands are thin wrappers\n  over IPC, and the React side has its own 925-test suite that llvm-cov does\n  not see at all.\n- `cascade-local-llm` (55.6%) cannot execute inference without multi-GB\n  weights. Every real-weights test is `#[ignore]`d — see T-P7-E19-08.\n\n`cascade-daemon` at 73.3% over 22.5k lines is the one worth attention. It is\nthe largest crate and the most behaviour-carrying, and unlike the others its\ngap is not mostly unreachable code.\n\n**What this number does not mean.** 76% of lines execute under test. It says\nnothing about whether the assertions on those lines are meaningful. This phase\nfound several tests that executed plenty of code and asserted nothing useful —\none re-implemented the algorithm it was testing inside the test body and kept\npassing through a full rewrite of the real one; another asserted that a struct\nfield it had just set to 50.0 was 50.0. Coverage would have counted both.\n\n## Baseline\n\n**76.32% line coverage at commit d8f1e96.** Future phases should compare\nagainst this figure and against the per-crate table, treating a fall in\n`cascade-core`, `cascade-daemon` or `cascade-rag` as more significant than the\nworkspace average moving.\n"
    },
    {
      "path": ".github/wiki/RAG-Pipeline.md",
      "content": "# RAG Pipeline\n\nCascade includes a local RAG (Retrieval-Augmented Generation) pipeline for semantic search over your instruction corpus. All processing runs on your machine. No data is sent to any external service.\n\n---\n\n## Overview\n\nThe pipeline has three tiers:\n\n| Tier | Method | Description |\n|---|---|---|\n| T1 FTS5 | Keyword | SQLite FTS5 full-text search over tokenized text |\n| T2 Dense | Embeddings | Multilingual E5 Large ONNX model, 1024-dimensional float vectors |\n| T3 RRF | Fusion | Reciprocal Rank Fusion merges T1 and T2 result lists |\n\nEach tier runs independently. The RRF step weights and merges them into a single ranked list.\n\n---\n\n## How it works\n\n1. **Ingest** - the daemon watches `.cascade/` directories for changes. When a file changes, the indexer reads it, splits it into chunks, and queues the chunks for indexing.\n\n2. **Chunking** - text is split by heading boundaries (`##` sections), with a max chunk size of 512 tokens. Overlap between chunks is 64 tokens.\n\n3. **Embedding** - each chunk is embedded using Multilingual E5 Large through fastembed's ONNX runtime, selected by `embedding_model = \"e5-large\"`. The old `bge-m3` key still works and means the same thing; it never ran native BGE-M3, which is why it was renamed. Model artifacts are cached under `~/.cascade/models/`, and embedding runs in a background thread pool.\n\n4. **Storage** - embeddings are stored in SQLite with the `sqlite-vec` extension. FTS5 tokens are stored in a separate virtual table in the same database.\n\n5. **Query** - a search query runs through both FTS5 and dense paths. RRF fuses the two ranked lists and returns the top-k results.\n\n---\n\n## Opt-in behavior\n\nRAG indexing is on by default. To disable it:\n\n```bash\ncascade config set rag.enabled false\ncascade daemon restart\n```\n\nWith RAG disabled, `cascade search` still works but uses keyword matching only (no embeddings).\n\n---\n\n## Disk usage\n\n| Component | Typical size |\n|---|---|\n| Multilingual E5 Large ONNX model | Downloaded once and stored in `~/.cascade/models/` |\n| SQLite database | ~2–20 MB per 100 instruction files |\n| FTS5 index | ~1–5 MB per 100 files |\n\nThe ONNX model is only loaded into memory when the daemon is running. On machines with less than 4 GB RAM, you can switch to a lighter model or disable dense embeddings entirely.\n\n---\n\n## Configuration\n\n```toml\n[rag]\nenabled = true\nembedding_model = \"e5-large\"\nfts_weight = 0.4\ndense_weight = 0.6\ntop_k = 10\n```\n\nSee the [Configuration](Configuration.md) page for all RAG keys.\n\n**Adjusting fusion weights:**\n\nIf you find keyword results more relevant than embedding results (common for short, precise queries), increase `fts_weight`:\n\n```bash\ncascade config set rag.fts_weight 0.6\ncascade config set rag.dense_weight 0.4\n```\n\nIf you find embedding results better (common for conceptual queries), do the opposite.\n\n---\n\n## Running a search\n\n```bash\ncascade search \"how do I handle authentication\"\ncascade search \"Tailwind component conventions\" --top 5\ncascade search \"database migration pattern\" --format json\n```\n\nResults show the matched text, the source file and tier, and the relevance score.\n\n---\n\n## Benchmarks\n\nThe RAG pipeline is benchmarked with Criterion.rs. Target latencies on an M-series Mac:\n\n| Operation | p50 |\n|---|---|\n| FTS5 query | < 5 ms |\n| Dense embedding (single chunk) | ~50 ms (CPU) |\n| RRF fusion over 100 results | < 1 ms |\n| End-to-end search (top 10) | ~60 ms |\n\nCI runs a regression gate: if a benchmark degrades more than 20% versus the baseline, the build fails. See [Performance](cascade-performance.md) for detailed benchmark data.\n\n---\n\n## Troubleshooting\n\n**Search returns no results**\n\nCheck that indexing completed:\n\n```bash\ncascade status\n# look for: index: N documents\n```\n\nIf the count is 0, try forcing a reindex:\n\n```bash\ncascade daemon restart\n```\n\n**Model download is slow**\n\nMultilingual E5 Large downloads on first use, selected by `embedding_model = \"e5-large\"` (or the deprecated `bge-m3` alias). Once cached in `~/.cascade/models/`, the model is not re-downloaded.\n\n**High memory usage**\n\nThe ONNX model keeps ~800 MB resident when loaded. If this is a problem, disable dense search:\n\n```bash\ncascade config set rag.dense_weight 0.0\ncascade config set rag.fts_weight 1.0\n```\n\nThis falls back to pure FTS5, which is fast and uses minimal memory.\n\nSee also: [Performance](cascade-performance.md) · [Configuration](Configuration.md) · [Troubleshooting](Troubleshooting.md)\n"
    }
  ],
  "queries": [
    {
      "query": "worth understanding rather chasing crate many small",
      "expect_top_path": "docs/coverage-baseline-2026-08-23.md"
    },
    {
      "query": "T-P7-E24-03 produce real measured coverage report record",
      "expect_top_path": "docs/coverage-baseline-2026-08-23.md"
    },
    {
      "query": "platform-gated trait-impl functions shows low function figure while",
      "expect_top_path": "docs/coverage-baseline-2026-08-23.md"
    },
    {
      "query": "window Access time right axis objects belonging stale",
      "expect_top_path": "docs/target-dir-hygiene.md"
    },
    {
      "query": "feature combination anything current build actually touches stays",
      "expect_top_path": "docs/target-dir-hygiene.md"
    },
    {
      "query": "single directory lock local-resource rule already forbids more",
      "expect_top_path": "docs/target-dir-hygiene.md"
    },
    {
      "query": "stdin stdout Read write stdio pipe-based data sources",
      "expect_top_path": ".github/wiki/Plugin-Development.md"
    },
    {
      "query": "ChatTool Add callable tools MCP server",
      "expect_top_path": ".github/wiki/Plugin-Development.md"
    },
    {
      "query": "EmbeddingProvider Replace default Multilingual Large embedder custom model",
      "expect_top_path": ".github/wiki/Plugin-Development.md"
    },
    {
      "query": "Results show matched text source file tier relevance score",
      "expect_top_path": ".github/wiki/RAG-Pipeline.md"
    },
    {
      "query": "Dense Embeddings Multilingual Large ONNX model dimensional float vectors",
      "expect_top_path": ".github/wiki/RAG-Pipeline.md"
    },
    {
      "query": "count try forcing reindex",
      "expect_top_path": ".github/wiki/RAG-Pipeline.md"
    },
    {
      "query": "success status Success ran duration next run computed schedule",
      "expect_top_path": ".github/wiki/Scheduler.md"
    },
    {
      "query": "Running Currently executing set transiently",
      "expect_top_path": ".github/wiki/Scheduler.md"
    },
    {
      "query": "max parallel Maximum tasks running simultaneously",
      "expect_top_path": ".github/wiki/Scheduler.md"
    }
  ]
}
