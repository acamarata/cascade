// Command cascade is the single Cascade binary: the CLI surface and, via
// `cascade daemon run`, the long-lived daemon. There is no separate daemon
// executable.
//
// The entrypoint is intentionally empty at the skeleton stage; the command tree
// is mounted here by Epic B.
package main

func main() {}
