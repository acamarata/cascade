Cascade is a local-first AI agent runtime: one binary that is both
the CLI surface and, via "cascade daemon run", the long-lived daemon.

Usage:
  cascade [flags]
  cascade [command]

Available Commands:
  approval       Inspect and decide queued approval requests
  backup         Create, list, restore, export, and import encrypted backups
  chat           Chat with the cascade assistant (interactive TUI or one-shot)
  ci             Run the local CI gate, or inspect recorded CI results
  completion     Generate shell completion scripts
  config         Inspect and edit cascade's config.toml
  context        Session-scope and context-engine commands
  daemon         Manage the cascade daemon process
  doctor         Diagnose the local cascade installation
  fleet          Inspect the local fleet of harness sessions
  github         Commands contributed by the cascade-github plugin
  help           Help about any command
  init           Set cascade up on this machine
  mcp            Serve the policy-filtered MCP tool registry
  memory         Remember, recall and forget memory records
  node           Manage this machine's participation in the node fleet
  pa             Personal-assistant bridge commands (Telegram, WhatsApp)
  pbd            PBD Engine
  plugin         Manage the plugin lifecycle: install, inspect, and remove
  policy         Explain and inspect policy decisions
  provider       Register and manage model providers
  recall         Search the retrieval index and print cited results
  run            Dispatch a one-shot or streaming model task through the conductor
  status         One-shot summary of the running daemon
  sync           Replicate domains between this device and its peers
  vault          Store and retrieve secrets in the platform keychain
  version        Print the cascade version, commit, build date, and install channel

Flags:
      --config string    override the config file path
  -h, --help             help for cascade
      --json             emit output as a versioned JSON envelope
      --no-color         disable colored output (also respects NO_COLOR; see docs/cli-output-contract.md)
      --profile string   select a named config profile
  -q, --quiet            suppress progress output
  -v, --verbose          increase log verbosity

Use "cascade [command] --help" for more information about a command.
