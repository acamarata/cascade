[package]
name        = "example-clock-widget"
version     = "0.1.0"
edition     = "2021"
description = "Annotated Widget example — renders current UTC time in a Cascade GUI panel."
authors     = ["Aric Camarata"]
license     = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
cascade-pdk = { path = "../../../crates/cascade-pdk" }
serde       = { version = "1", default-features = false, features = ["derive", "alloc"] }
serde_json  = { version = "1", default-features = false, features = ["alloc"] }

[dev-dependencies]
cascade-pdk = { path = "../../../crates/cascade-pdk", features = ["test-harness"] }
serde_json  = "1"
