{
  "name": "cascade",
  "version": "0.9.1",
  "private": true,
  "description": "Cascade workspace root — JS apps live under apps/ (Rust workspace is at the repo root via Cargo.toml).",
  "packageManager": "pnpm@10.30.1"
}
